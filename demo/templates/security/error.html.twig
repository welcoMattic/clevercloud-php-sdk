{% extends 'base.html.twig' %}

{% block title %}Erreur OAuth — Clever Cloud Dashboard{% endblock %}
{% block page_title %}Échec du flow OAuth{% endblock %}

{% block body %}
    <div class="card">
        <div class="card__body">
            <p class="card__error">{{ exception.getMessage() }}</p>
            <div class="btn-group" style="margin-top: 1rem;">
                <a class="btn btn--primary" href="{{ path('login') }}">Réessayer</a>
            </div>
        </div>
    </div>
{% endblock %}
