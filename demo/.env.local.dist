# Copy to .env.local and fill in your Clever Cloud OAuth consumer credentials.
# Get them from https://www.clever.cloud/doc/extend/cc-api/

# Only the consumer key / secret are needed up front — the user access token
# is obtained at login time via the OAuth 1.0a 3-legged flow.
CC_CONSUMER_KEY=
CC_CONSUMER_SECRET=
